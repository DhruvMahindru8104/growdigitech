/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['i.pinimg.com', 'growdigitech.vercel.app', 'growdigitech.com','media.licdn.com']
    }
};

export default nextConfig;
